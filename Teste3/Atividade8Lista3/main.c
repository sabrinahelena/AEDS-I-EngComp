#include <stdio.h>
#include <stdlib.h>

int main()
{
    int x=1, soma;
    float resultado;
    while(x<=49){
        soma+= x;
        x++;
    }
    printf("A soma de 1 ate 49 e': %d ", soma);
    return 0;

}
